import './nav.css';

// Material UI
import { AppBar } from '@material-ui/core'

function Nav(){
    return(
        <AppBar className="nav">
            <h1>Meeting Rooms Management System</h1>
        </AppBar>
    )
}

export default Nav